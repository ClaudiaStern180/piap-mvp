import { PageHeader } from "@/components/PageHeader";

export default function AdminUsersPage() {
  return (
    <div>
      <PageHeader title="Admin / Users" />
      <p>Connect this page to your real identity source or local user table. The role model is already defined: clerk, po, admin.</p>
    </div>
  );
}
